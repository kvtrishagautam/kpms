<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leave Application (Annual Leave)</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding: 20px;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            padding: 15px;
            background: white;
            border-bottom: 2px solid #ffd700;
        }

        .nav-items {
            display: flex;
            gap: 20px;
        }

        .nav-icons {
            display: flex;
            gap: 10px;
        }

        .breadcrumb {
            padding: 15px;
            background: white;
            margin: 20px 0;
        }

        .container {
            background: white;
            padding: 30px;
            border-radius: 8px;
            max-width: 800px;
            margin: 0 auto;
        }

        .form-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
        }

        input[type="text"],
        input[type="date"],
        input[type="number"],
        select,
        textarea {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: #f8fafc;
        }

        .date-inputs {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .file-input {
            margin-top: 5px;
        }

        .buttons {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 30px;
        }

        .btn-submit {
            background: #2e7d32;
            color: white;
            padding: 12px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .btn-reset {
            background: white;
            color: #dc3545;
            padding: 12px;
            border: 1px solid #dc3545;
            border-radius: 4px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="nav-items">
            <div>Dashboard</div>
            <div>Requests</div>
            <div>Payroll</div>
            <div>Company</div>
            <div>Extras</div>
        </div>
        <div class="nav-icons">
            <span>🔔</span>
            <span>✉</span>
            <span>👤</span>
        </div>
    </nav>

    <div class="breadcrumb">
        Dashboard > Apply for Leave > Annual Leave
    </div>

    <div class="container">
        <% if (error) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>

        <div class="form-header">
            <h2>📖 Leave Application</h2>
            <p>Fill the required fields below to apply for leave.</p>
        </div>

        <!-- Update the form to handle file uploads -->
        <form action="/leave/apply" method="POST" >
            <div class="form-group">
                <label>Leave Type</label>
                <input type="text" id="leave_type" name="leave_type" required>
            </div>

            <div class="date-inputs">
                <div class="form-group">
                    <label>Start Date</label>
                    <input type="date" name="start_date" id="start_date" required>
                </div>

                <div class="form-group">
                    <label>End Date</label>
                    <input type="date" id="end_date" name="end_date" required>
                </div>
            </div>

            <div class="date-inputs">
                <div class="form-group">
                    <label>Duration (days)</label>
                    <input type="number" id="duration" name="duration" required>
                </div>

                <div class="form-group">
                    <label>Resumption Date</label>
                    <input type="date" id="resumption_date" name="resumption_date" required>
                </div>
            </div>

            <div class="form-group">
                <label>Reason for leave</label>
                <textarea name="reason" id="reason" rows="4" required></textarea>
            </div>

            <div class="form-group">
                <label>Attach handover document (pdf, jpg, docx or any other format)</label>
                <input type="file" name="handover_document" class="file-input">
            </div>

            <div class="buttons">
                <button type="submit" class="btn-submit">Submit</button>
            </div>
        </form>
    </div>
</body>
</html>